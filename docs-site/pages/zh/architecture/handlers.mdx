# Handler 模式

深入了解模型推理的策略模式实现。

---

## 🎯 概述

Handler 系统实现了**策略模式**，允许不同的 AI 模型通过统一接口互换使用。

```python
# 使用示例 - 相同接口，不同实现
handler = registry.get_handler("yolov8n.pt")  # 返回 YOLOHandler
handler = registry.get_handler("Salesforce/blip-vqa-base")  # 返回 BLIPVQAHandler

# 无论模型类型，API 相同
model, processor = handler.load(model_id)
result = handler.infer(model, processor, image, **params)
```

---

## 🏗️ BaseHandler 接口

所有 Handler 继承自 `BaseHandler`，必须实现两个抽象方法：

```python
class BaseHandler(ABC):
    """所有模型 Handler 的抽象基类。"""

    def __init__(self, device: str = "cpu"):
        self.device = device

    @abstractmethod
    def load(self, model_id: str) -> Tuple[Any, Optional[Any]]:
        """加载模型和可选处理器。"""
        pass

    @abstractmethod
    def infer(self, model, processor, image, **params) -> dict:
        """对图像运行推理。"""
        pass

    def make_result(self, image, detections, task, **kwargs) -> dict:
        """创建标准化结果字典。"""
        return {
            "width": image.shape[1],
            "height": image.shape[0],
            "task": task,
            "detections": detections,
            **kwargs
        }
```

---

## 📦 Handler 实现

| Handler | 模型 | 任务 |
|---------|--------|------|
| YOLOHandler | YOLOv8 (n/s/m/l/x) | 检测、分割、姿态 |
| DETRHandler | facebook/detr-* | 目标检测 |
| OWLViTHandler | google/owlvit-* | 开放词汇检测 |
| GroundingDINOHandler | grounding-dino-* | 文本提示检测 |
| BLIPCaptionHandler | Salesforce/blip-* | 图像描述 |
| BLIPVQAHandler | Salesforce/blip-vqa-* | 视觉问答 |

---

## 🔗 相关文档

- [架构概述](/zh/architecture/overview) — 高层系统设计
- [添加模型](/zh/guides/adding-models) — 分步指南
- [REST API](/zh/api/rest-api) — API 端点参考
