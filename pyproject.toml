[tool.black]
line-length = 100
target-version = ["py311"]

[tool.isort]
profile = "black"
line_length = 100
force_single_line = false

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "UP"]
ignore = ["E203", "E266", "E501"]

[tool.pytest.ini_options]
addopts = "-q"
